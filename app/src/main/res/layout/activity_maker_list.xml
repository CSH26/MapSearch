<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.example.tj.mapsearch.MakerList.MakerListActivity">

    <LinearLayout
        android:orientation="horizontal"
        android:id="@+id/listLayout"
        android:layout_weight="9"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ListView
            android:fastScrollEnabled="true"
            android:divider="@color/colorAccent"
            android:headerDividersEnabled="false"
            android:dividerHeight="2dp"
            android:listSelector="@color/list_item_color"
            android:id="@+id/makerListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        </ListView>
    </LinearLayout>

    <LinearLayout
        android:visibility="gone"
        android:layout_weight="1"
        android:layout_below="@+id/listLayout"
        android:gravity="center"
        android:background="@drawable/button_back"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/slidingLayout"
        android:orientation="horizontal">

        <Button
            android:layout_margin="10dp"
            android:layout_gravity="center"
            android:background="@drawable/selector_button_click"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="삭제"
            android:id="@+id/deleteMaker" />

        <Button
            android:layout_margin="10dp"
            android:layout_gravity="center"
            android:background="@drawable/selector_button_click"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="맵으로 이동"
            android:id="@+id/moveMaker" />

    </LinearLayout>

</LinearLayout>
